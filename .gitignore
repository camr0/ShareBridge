# Git worktrees
.worktrees/

# Brainstorming mockups
.superpowers/

# Go build and test artifacts
.gocache/
*.test
*.out
*.prof
coverage.out
coverage.txt

# Local runtime data
pb_data/

# FRP release cache (downloaded and checksum-verified by relay/scripts/fetch-frp.sh;
# binaries are never committed — release packaging bundles verified executables)
relay/.cache/

# Local secrets (never committed)
.env
.env.*
# ...except the committed template (cp it to .env.testing and fill in real values)
!.env.testing.example

# Common local build outputs
bin/
dist/
control/server
agent/opencloudshare
agent/agent
agent/benchdirect
agent/frpc

# macOS + editor/agent tooling artifacts
.DS_Store
.pi/
