# TURN server configuration
# IMPORTANT: TURN_HOST must be your VPS's PUBLIC IP or domain name
# Browsers receive this in ICE config and connect directly to it
# Do NOT use Docker hostnames like "coturn" - browsers can't resolve them
TURN_HOST=your-vps-public-ip-or-domain.com
TURN_SECRET=your-turn-secret-here
